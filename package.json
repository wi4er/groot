{
  "name": "groot",
  "version": "0.10.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest . --verbose --testEnvironment=node --watchAll --runInBand",
    "leak": "jest . --verbose --testEnvironment=node --runInBand --detectOpenHandles",
    "coverage": "jest . --verbose --testEnvironment=node --runInBand --coverage",
    "start": "node ."
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.17.1",
    "express-jwt": "^6.1.0",
    "mongoose": "^6.0.4",
    "redis": "^3.1.2"
  },
  "devDependencies": {
    "jest": "^27.1.0",
    "nodemon": "^2.0.12",
    "supertest": "^6.1.6"
  }
}
